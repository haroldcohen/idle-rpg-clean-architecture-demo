location / {
    root /home/frontend/current/build;

    try_files $uri /index.html;

    add_header cache-control "no-store";

    location /static/ {
        try_files $uri =404;

        add_header cache-control "public, max-age=31536000, immutable";
    }
}
